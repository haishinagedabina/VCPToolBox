# WeWriteHotspots 配置 (可选)
#
# 多数情况下保持默认即可。如需自定义请复制到 config.env 并修改。

# 默认返回的热点条数 (Agent 未传 limit 时使用)
# 取值范围: 1-50
WEWRITE_HOTSPOTS_DEFAULT_LIMIT=20

# 是否允许走系统代理 (默认否, 因为国内三大平台直连更稳)
# 仅当你的网络环境必须经过代理才能访问微博/头条/百度时开启
# WEWRITE_ALLOW_PROXY=true
